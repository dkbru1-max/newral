Используй docs/PROJECT_BRIEF.md как источник истины.
Пиши, используя лучшие практики и изящные решения, никуда не спеши, делай всё аккуратно и проверяй код.

Нужно привести репозиторий к стандартам "Kubernetes-ready", оставаясь на Docker Compose для MVP:
1) Для всех будущих сервисов зафиксировать единый стандарт:
   - конфиг через env vars (+ .env.example)
   - логи только в stdout/stderr
   - endpoints: /healthz и /readyz
   - корректный SIGTERM shutdown
2) Добавить docs/SERVICE_CONVENTIONS.md со стандартом (порты, таймауты, ошибки, версии API).
3) Добавить папку deploy/k8s/ (пока пустые шаблоны) и deploy/helm/ (папка-скелет).
4) Добавить docs/DEPLOYMENT.md: как dev запускается через compose, а prod — через k8s (позже).

Не добавляй сложную бизнес-логику. Нужны только каркасы и стандарты.
