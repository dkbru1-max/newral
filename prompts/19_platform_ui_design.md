Описание: Дизайн интерфейса административного портала распределённой платформы Newral (развёртываемого локально или в облаке). Портал предназначен для управления системой и мониторинга её работы. Необходимо создать современный, удобный интерфейс, соответствующий потребностям разных категорий пользователей и включающий все ключевые функции платформы.

Аудитория

Партнёры: заинтересованные стороны, которые оценивают эффективность платформы.

Инвесторы: наблюдают за ключевыми показателями и прогрессом проекта.

Системные администраторы: настраивают инфраструктуру, следят за состоянием нод и системой безопасности.

Разработчики: запускают проекты, интегрируют решения и анализируют результаты работы агентов.

Основные задачи портала

Настройка нод: добавление, конфигурирование и управление узлами (нодами) распределённой сети.

Просмотр задач: список текущих и завершённых задач, их статусы, результаты, возможность фильтрации и поиска.

Мониторинг агентов: наблюдение за состоянием подключённых агентов (онлайн/офлайн, загрузка, ошибки).

Режим работы AI: переключение режимов AI-модерации (уровни от OFF до AI_FULL) для контроля степени автономности ИИ.

Запуск проектов: интерфейс для инициации новых проектов/задач, настройка параметров перед запуском.

Просмотр статистики и логов: дашборды с ключевыми метриками, история событий и централизованный лог-центр для отладки (с возможностью фильтровать по уровню: информация, предупреждение, ошибка).

Настройка безопасности: управление пользователями, правами доступа, а также конфигурация сертификатов, ключей API и т.п.

Интеграции: настройка интеграции с внешними сервисами (например, мониторинг, оповещения, CI/CD) для расширения возможностей платформы.

Обязательные возможности интерфейса

Адаптивный дизайн: корректное отображение на любых размерах экрана (от больших мониторов до мобильных устройств).

Поддержка тем: светлая и тёмная тема оформления, переключение темы пользователем; единая система переменных для цветов и отступов.

Закрепляемые элементы: фиксированное главное меню и важные кнопки (например, кнопка отправки новой задачи) остаются видимыми при прокрутке.

Микроанимации: небольшие анимационные эффекты для пользовательских действий (нажатия кнопок, индикаторы загрузки, изменение статусов) для улучшения отзывчивости интерфейса.

Скелетная загрузка: отображение плейсхолдеров (skeleton screens) при загрузке данных, чтобы пользователь видел структуру контента и не ждал на пустом экране.

Автообновление данных: динамическое обновление показателей и списков без перезагрузки страницы (через WebSocket/SSE) для отображения актуальной информации в реальном времени.

Основные компоненты интерфейса

Дашборд: обзорная панель с ключевыми показателями системы (количество активных агентов, выполняющихся задач, состояние нод и пр.), графики производительности и диаграммы прогресса.

Список задач: таблица или карточки задач с информацией о каждой задаче (название, проект, ответственный агент, статус, прогресс, время запуска/завершения). Предусмотреть пагинацию или ленивая подгрузка, фильтры по статусу, поисковую строку.

Агенты: раздел со списком агентов (клиентских узлов) – отображение имени/ID агента, текущего статуса соединения (online/offline), нагрузки (например, процент использования), выполняемых задач, версии ПО. Возможность просмотреть детали агента и логи, перезагрузить или отключить при необходимости.

Проекты: управление проектами, к которым принадлежат задачи. Список проектов с описанием, статистикой (сколько задач, участники, общий прогресс). Опция создать новый проект (с выпадающим меню параметров) – должна быть заметная кнопка «Создать проект».

Лог-центр: центральное хранилище логов и событий системы. Интерфейс для просмотра и фильтрации логов по типу (инфо, предупреждение, ошибка) и источнику (агент, система, задача). Цветовое кодирование строк лога в зависимости от уровня: например, зелёный для успешных операций, жёлтый для предупреждений, красный для ошибок.

Настройки: страница настроек платформы – профиль организации, управление пользователями (добавление/удаление, роли), параметры безопасности (аутентификация, шифрование), лимиты ресурсов, интеграции с внешними сервисами. Настройки сгруппированы по категориям с использованием вкладок или аккордеона для удобства навигации.

Панель AI-модерации: специальный раздел для управления режимами работы AI. Отображает текущий режим (OFF, частичный, полный AI) и позволяет его менять. Здесь же могут показываться рекомендации ИИ или запросы на подтверждение действий (если режим не полный). Визуально может быть представлен как переключатель (toggle/switch) или селектор режима с пояснениями каждого уровня.

Современные UX-фичи

Кнопка «наверх» с прогресс-индикатором: плавающая кнопка для быстрого возврата к началу страницы. Имеет кольцевой или линейный индикатор, показывающий процент прокрутки страницы: по мере прокрутки индикатор заполняется, давая визуальную подсказку длины страницы.

Кнопка «Создать проект» с анимацией: при нажатии на кнопку запуска нового проекта вместо мгновенного переключения состояния – короткая анимация (например, индикатор загрузки внутри кнопки или изменение текста на «Запуск...») для явного подтверждения действия. После успешного создания – визуальный отклик (например, зелёная галочка или всплывающее уведомление).

Ленивая подгрузка списков: для списков задач, логов или агентов реализовать подгрузку элементов по мере прокрутки (infinite scroll) либо подгрузку по нажатию кнопки «Загрузить ещё». Это позволяет быстрее отобразить первые элементы и снизить нагрузку.

Поддержка тёмной темы: кроме светлой темы, полная версия в тёмных тонах. Переключатель темы доступен в шапке или подвале. Все элементы UI (текст, иконки, фон, графики) корректно адаптируются под обе темы.

Важные элементы UI и детализация

Отображение статуса агентов: рядом с каждым агентом – цветовой индикатор состояния (например, зелёный кружок для онлайн, серый или красный для офлайн, жёлтый для проблем). Статус дублируется текстом/иконкой с подсказкой по наведению (tooltip), чтобы было понятно даже цветовым незрячим пользователям.

Цветовое кодирование логов: каждая запись лога помечена цветом или иконкой в зависимости от уровня события. Успех – зелёный (🟢 или иконка галочки), Предупреждение – жёлтый (🟡 или восклицательный знак), Ошибка – красный (🔴 или крестик). Это позволяет быстро сканировать журнал и находить проблемы.

Таблицы с фиксированными заголовками: в списках задач, агентов, логов – реализовать таблицы с закреплённой шапкой. При скролле вниз заголовки колонок остаются видимыми. Ширина колонок выравнивается, длинные тексты обрезаются с многоточием и всплывающей подсказкой. Предусмотреть фильтрацию и сортировку по столбцам (например, по алфавиту, дате, статусу).

Инструменты и лучшие практики

Библиотеки компонентов: использовать современные UI-киты, такие как Material UI, Tailwind UI, shadcn/ui (или их аналоги для выбранного фреймворка, будь то React, Yew, Tauri и др.). Они обеспечивают согласованный стиль и проверенные паттерны дизайна, что ускоряет разработку.

Адаптивная сетка: применить гибкие сетки и utility-классы (например, из Tailwind CSS) для построения компоновки. Компоненты должны автоматически растягиваться или перестраиваться (например, из таблицы в карточки) под узкие экраны.

UX-паттерны: придерживаться стандартных подходов в UX – понятная навигация (боковое или верхнее меню), единый стиль кнопок и форм, модальные окна для подтверждения опасных действий, уведомления о результатах операций (toasts).

Доступность: проверить, что все интерактивные элементы имеют явные фокусные состояния (outline или подсветку для фокуса клавиатурой), для изображений предусмотрены alt-теги, цвета обеспечивают достаточный контраст. Использовать атрибуты ARIA там, где это необходимо для улучшения совместимости со скринридерами.

Favicon и брендирование

В интерфейсе отобразить фирменный стиль Newral – использовать логотип (например, компактная версия логотипа в шапке портала) и цветовую палитру бренда.

Favicon: добавить иконку для браузера, изготовленную на основе логотипа Newral. Можно использовать файл логотипа newral_big_logo.png, адаптировав его под маленький размер (например, выделив символ или упрощённую графику). Фавикон должен быть хорошо различим на тёмной и светлой теме браузера.

Пример компонента (React + Tailwind)

Ниже приведён пример реализации фрагмента UI с использованием React и Tailwind CSS, демонстрирующий некоторые описанные требования – таблицу агентов с закреплённым заголовком, цветовой индикацией статуса и прокручиваемой областью:

// Список агентов с прокручиваемой таблицей и цветными статусами
<div className="overflow-y-auto max-h-64 border rounded">
  <table className="min-w-full text-left text-sm">
    <thead className="sticky top-0 bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-200">
      <tr>
        <th className="px-3 py-2">Агент</th>
        <th className="px-3 py-2">Статус</th>
        <th className="px-3 py-2">Задач</th>
      </tr>
    </thead>
    <tbody className="divide-y divide-gray-200 dark:divide-gray-700">
      <tr>
        <td className="px-3 py-2">Agent 1</td>
        <td className="px-3 py-2">
          <span className="text-green-600 font-medium">online</span>
        </td>
        <td className="px-3 py-2">5</td>
      </tr>
      <tr>
        <td className="px-3 py-2">Agent 2</td>
        <td className="px-3 py-2">
          <span className="text-red-600 font-medium">offline</span>
        </td>
        <td className="px-3 py-2">0</td>
      </tr>
      {/* ...другие агенты */}
    </tbody>
  </table>
</div>


Этот пример демонстрирует использование sticky header (position: sticky у <thead>), цветовое оформление статусов (text-green-600 для online, text-red-600 для offline), а также адаптацию к тёмной теме через классы dark:*. Подобные приёмы обеспечивают удобство чтения таблиц с большим количеством данных.